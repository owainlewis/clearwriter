# Documents are built in-memory in test setup because public_token is
# generated by a before_validation callback and title by before_save;
# fixtures skip callbacks and the unique-token index would collide.
